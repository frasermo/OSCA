# Contributing to OSCA (openScope Traveller)

We welcome issues, pull requests, and doc fixes. Please:
1. Open a GitHub Issue describing the change.
2. For doc changes, update both the **Wiki** (mirrored in `docs/`) and any images under `images/`.
3. Follow style guides in `docs/project/DOCS_STYLE.md`.
